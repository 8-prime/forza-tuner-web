import { TuningDataTransmission } from './tuning-data-transmission';

describe('TuningDataTransmission', () => {
  it('should create an instance', () => {
    expect(new TuningDataTransmission()).toBeTruthy();
  });
});
